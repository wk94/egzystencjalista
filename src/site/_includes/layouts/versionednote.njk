<!DOCTYPE html>
<html lang="pl">
  <head>
    <title>{{ item.title }}</title>
    {%include "components/pageheader.njk"%}
  </head>
  <body>
    <div class="content">
      {%- if item.tags !== 'Garden' -%}
        <a href="/">🏡 Strona główna</a>
        {%endif%}
        {{ item.content | safe}}
      </div>
    </body>
  </html>
